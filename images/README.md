Drop your real photos here to replace the placeholders:

- /images/timeline/01.jpg ... 05.jpg  -> used by the Timeline section
- /images/gallery/01.jpg ... 08.jpg   -> used by the Memory Gallery section

Filenames must match what's set in src/data/content.ts (or update the paths there).
If an image is missing, the site gracefully hides that image slot instead of breaking.
